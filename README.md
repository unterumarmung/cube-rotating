# Rotating Cube with Quaternions

## Dependencies

* Python 3.8
* `numpy` library
* `PyOpenGL` library
* `pygame` library

### Installing library dependencies

```
pip install -r requirements.txt
```

